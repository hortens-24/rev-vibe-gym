{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch (WSL gdb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}", // ← 同じく
      "cwd": "${fileDirname}",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "stopAtEntry": false,
      "externalConsole": false,
      "preLaunchTask": "build active file"
    }
  ]
}
